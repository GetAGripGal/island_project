[package]
name = "island_project"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 2

[profile.release]
opt-level = 3

[dependencies]
bevy = "0.6"
bevy_egui = "0.12"
hashbrown = "0.12"

[dev-dependencies]
bevy = { version = "0.6", features = ["dynamic"] }